@model BarcodeTrackerWEB.Models.Order

<div>
    @{
        if (Model.PurchaseOrderDoc != null)
        {

            string imageBase64 = Convert.ToBase64String(Model.PurchaseOrderDoc, 0, Model.PurchaseOrderDoc.Length);
            //string imageSrc = string.Format("data:application/pdf;base64, {0}", imageBase64);
            @*<img src="@imageSrc" class="img-responsive" style="width:100%; height:100%;" />*@

    <object>
        <embed src="data:application/pdf, base64, imageBase64" type="application/pdf" width="1024" height="768" />
    </object>
            

        }

    }
</div>

<div>

    <script src="~/Scripts/jquery-3.2.1.js"></script>


</div>